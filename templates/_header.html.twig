<header class="bg-[#14213D]">
	<nav class="flex justify-between items-center w-11/12 mx-auto py-4">
		<img class="w-16 cursor-pointer" src="{{ asset('img/home/logo.png') }}" alt="Logo">


		<div class="flex w-full md:w-auto md:flex-row flex-col items-center px-5 bg-[#14213D]">
			<ul id="main-nav" class="flex md:flex-row flex-col md:items-center md:gap-16 gap-8">
				<li>
					<a class="text-white hover:text-[#b49047]" href="{{ path('home') }}">Accueil</a>
				</li>
				<li>
					<a class="text-white hover:text-[#b49047]" href="{{ path('app_service') }}">Services</a>
				</li>
				<li>
					<a class="text-white hover:text-[#b49047]" href="{{ path('app_about') }}">À propos</a>
				</li>
				<li>
					<a class="text-white hover:text-[#b49047]" href="{{ path('app_faq') }}">Faq</a>
				</li>
				<li>
					<a class="text-white hover:text-[#b49047]" href="{{ path('app_contact') }}">Contact</a>
				</li>

				{% if app.user %}
					{% if 'ROLE_ADMIN' in app.user.roles %}
						<li>
							<a class="text-white hover:text-[#b49047]" href="{{ path('app_adminpage') }}">Espace Admin</a>

						</li>
					{% elseif 'ROLE_USER' in app.user.roles %}
						<li>
							<a class="text-white hover:text-[#b49047]" href="{{ path('app_student') }}">Espace Étudiant</a>

						</li>
					{% endif %}


					<li>
						<a class="text-white hover:text-[#b49047]" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				{% else %}

					<li>
						<a class="text-white hover:text-[#b49047]" href="{{ path('app_login') }}">Se connecter</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</nav>
</header>
