{{ form_start(form) }}
<div class="space-y-4">
	{{ form_widget(form, {'attr': {'class': 'form-input block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500'}}) }}
</div>
<div class="mt-6">
	<button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
		{{ button_label|default('Save') }}
	</button>
</div>
{{ form_end(form) }}

