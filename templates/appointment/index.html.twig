{% extends 'base.html.twig' %}

{% block title %}Appointment index{% endblock %}

{% block body %}
    <h1>Appointment index</h1>

    <table class="table">
<p>
	<strong class="text-[#14213D]">Prénom :</strong>



</p>

        <thead>
            <tr>
                <th>Id</th>
                <th>Date_update</th>
                <th>Object</th>
                <th>Creation_date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for appointment in appointments %}
            <tr>
                <td>{{ appointment.id }}</td>
<td>
{{ appointment.user.email}}
{{ appointment.user.profile.name}}
{{ appointment.user.profile.postname}}</td></td>






                <td>{{ appointment.dateUpdate ? appointment.dateUpdate|date('Y-m-d') : '' }}</td>
                <td>{{ appointment.object }}</td>
                <td>{{ appointment.creationDate ? appointment.creationDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_appointment_show', {'id': appointment.id}) }}">show</a>
                    <a href="{{ path('app_appointment_edit', {'id': appointment.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_appointment_new') }}">Create new</a>
{% endblock %}
