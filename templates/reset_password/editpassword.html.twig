{% extends 'base.html.twig' %}

{% block title %}Changer le mot de passe
{% endblock %}

{% block javascripts %}
	{% block importmap %}
		{{ importmap(['app', 'reset']) }}
	{% endblock %}
{% endblock %}

{% block body %}
	<div class="flex items-center justify-center min-h-screen bg-gray-100 px-4">
		<div class="bg-white shadow-lg rounded-lg p-6 w-full max-w-md sm:p-8">
			<h1 class="text-2xl font-bold text-center text-blue-900 mb-6">Changer votre mot de passe</h1>

			{% for message in app.flashes('success') %}
				<div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4" role="alert">
					{{ message }}
				</div>
			{% endfor %}

			{{ form_start(changePasswordForm, {'attr': {'class': 'space-y-4 sm:space-y-6'}}) }}

			<div class="relative">
				{{ form_label(changePasswordForm.plainPassword.first, 'New password', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1 sm:mb-2'}}) }}
				{{ form_widget(changePasswordForm.plainPassword.first, {'attr': {'class': 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm'}}) }}
				{{ form_errors(changePasswordForm.plainPassword.first) }}
				<span class="absolute right-3 top-9 cursor-pointer" id="togglePasswordVisibility">
					👁️‍🗨️
				</span>
			</div>

			<div class="relative">
				{{ form_label(changePasswordForm.plainPassword.second, 'Repeat Password', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1 sm:mb-2'}}) }}
				{{ form_widget(changePasswordForm.plainPassword.second, {'attr': {'class': 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm'}}) }}
				{{ form_errors(changePasswordForm.plainPassword.second) }}
				<span class="absolute right-3 top-9 cursor-pointer" id="togglePasswordVisibility">
					👁️‍🗨️
				</span>
			</div>

			<button type="submit" class="w-full py-2 px-4 bg-custom-blue text-white font-semibold rounded-lg shadow-sm hover:bg-custom-login transition-colors duration-200">
				Changer le mot de passe
			</button>

<button type="button" onclick="window.history.back();" class="w-full py-2 px-4 bg-custom-blue text-white font-semibold rounded-lg shadow-sm hover:bg-custom-login transition-colors duration-200">

	Annuler
</button>



			{{ form_end(changePasswordForm) }}
		</div>
	</div>
{% endblock %}

