{% extends 'base.html.twig' %}

{% block title %}Modifier les documents
{% endblock %}

{% block body %}
	<div class="mt-40">
		<h1>Modifier le document</h1>

		{{ include('documents/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('app_documents_index') }}">Retour à la liste</a>

		{{ include('documents/_delete_form.html.twig') }}
	</div>
{% endblock %}

